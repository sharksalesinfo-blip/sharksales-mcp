import ComingSoon from '../ComingSoon';

export default function BrandsAdmin() {
  return (
    <ComingSoon
      title="Brands"
      note="Manage brand info (name, tagline, description, hero image). Admin only."
    />
  );
}
